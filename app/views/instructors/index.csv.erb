<%- headers = ['First Name', 'Middle Name', 'Last Name', 'UIN', 'NetID', 'Email'] -%>
<%= CSV.generate_line headers %>
<%- @instructors.each do |inst| -%>
<%= CSV.generate_line([inst.first_name, inst.middle_name, inst.last_name, inst.uin, inst.netID, inst.email]) -%>
<%- end -%>